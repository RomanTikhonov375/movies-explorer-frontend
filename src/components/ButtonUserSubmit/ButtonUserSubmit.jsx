import React from 'react';

function ButtonUserSubmit({buttonText}) {
   
        return (
           <>
           <button type='submit' className='buttonUserSubmit'>{buttonText}</button>
           </>
        );
    
}

export default ButtonUserSubmit;